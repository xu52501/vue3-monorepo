# monorepo模板

```bash
基于vue的monorepo模板
```

# Vue 3 + TypeScript + tailwind + antdv + Vite + turbo

# 运行

```bash
# 安装依赖
pnpm install

# 运行
pnpm run dev 启动所有项目

# 构建
pnpm run build
批量构建会有问题，进入子项目进行构建
```
